import React from 'react';
import "./Add.css";

const Add = (props) => {
    return (
        <div className='header'>
            <button className='add_btn' >ADD NEW EXPENSE</button>
        </div>
    );
};

export default Add;